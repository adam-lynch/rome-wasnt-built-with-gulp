# Gulp = [Streams](https://github.com/substack/stream-handbook) + Orchestrator + Vinyl + tasks

```
gulp.task(name, doSomething);
```

```
gulp.src(globs);
```

```
gulp.dest(path);
```

```
gulp.watch(globs, tasks);
```