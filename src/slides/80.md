# Gulp = [Streams](https://github.com/substack/stream-handbook) + tasks

```
gulp.task(name, doSomething);
```

```
gulp.src(pathOrPaths);
```

```
gulp.dest(path);
```

```
gulp.watch(pathOrPaths, tasks);
```